import styled from 'styled-jss';

let MosaicWindow = styled('div')({

})

let MosaicWindowBody = styled('div')({
  borderTopWidth: 0,
  background: "#f5f8fa",
  borderBottomRightRadius: 3,
  borderBottomLeftRadius: 3,
})

let MosaicWindowBodyOverlay = styled('div')({
  
})

let MosaicWindowActionsBar = styled('div')({
  
})

let MosaicWindowPreview = styled('div')({

})

let MosaicWindowToolbar = styled('div')({

  :hover: {background: linear-gradient(to bottom, #394b59, #30404d)};
})

let MosaicWindow = styled('div')({

})

const SplitLine = 'box-shadow: 0 0 0 1px #2b95d6;'


split (line)
position: absolute;
z-index: 1;

row
margin-left: -3px;
    width: 6px;
    cursor: ew-resize;


  COLUMN_LAYOUT
  