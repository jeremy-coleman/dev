import * as React from 'react';

/**
 * Changes the cursor when visible
 */
export default class CursorOverlay extends React.Component {

    render() {
        return (
            <div className='cursor-overlay'></div>
        );
    }
}