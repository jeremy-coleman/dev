import * as React from 'react'
import {observer} from 'mobx-react'
import {DesignerApp} from '../workflow-designer/components/workflow-toolbar/designer-screen'

export let CloudPage = observer((props) =>
<div>
<DesignerApp/>
</div>
)
