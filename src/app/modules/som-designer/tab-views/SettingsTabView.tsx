import * as React from 'react'
import {observer} from 'mobx-react'
import {withTheme} from 'theming'
import styled from 'styled-jss'


export let SettingsTabView = withTheme(observer((props) =>
<div>
  
</div>
))
